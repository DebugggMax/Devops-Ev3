apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-ventas
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend-ventas
  template:
    metadata:
      labels:
        app: backend-ventas
    spec:
      containers:
      - name: backend-ventas
        image: 956050146632.dkr.ecr.us-east-1.amazonaws.com/tienda-ventas:latest
        ports:
        - containerPort: 8082
        env:
        - name: DB_ENDPOINT
          value: "mysql-svc"  # Apunta al nombre del Service de arriba
        - name: DB_PORT
          value: "3306"
        - name: DB_NAME
          value: "innovatech_db"
        - name: DB_USERNAME
          value: "root"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: password